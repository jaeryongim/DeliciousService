<p><%= flash[:alert] %></p>
<h2>맛집 정보 보기</h2>

<div style="border: 1px solid #ffffff">
  <h4>제목 : <%= @post.title %></h4>
  <p style="color: #ff0000">분류 : <%= @post.category %></p>
  <p>내용 : <%= @post.content %></p>
  <p><%= link_to '수정', "/foods/edit/#{@post.id}" %> | <%= link_to '삭제', "/foods/delete_complete/#{@post.id}",
                                                                  confirm: {message: '정말로 지우시겠습니까?'} %></p>
  <hr/>
  <% @post.comments.each do |comment| %>
      <p><strong><%= comment.user.username %></strong> <%= comment.content %> |
                              <%= link_to '삭제', "/foods/delete_comment_complete/#{comment.id}" %></p>
  <% end %>
  <%= form_tag '/foods/write_comment_complete', method: 'post' do %>
      <input type="hidden" name="post_id" value="<%= @post.id %>">
      <% if !@comment_writer.nil? %>
          <p><strong><%= @comment_writer.username %></strong>: <input type="text" name="comment_content" placeholder="댓글 내용을 입력하세요.">
      <% end %>
      <%= submit_tag '달기' %></p>
  <% end %>
  <p><%= link_to '전체 리스트 가기', '/' %></p>
</div>